# 项目级开发规则

本文件作用于整个仓库。任何自动化开发代理或协作者在修改项目时都必须遵守。

## 提交规则

1. 每完成一个可独立描述、可独立验证的功能或修复，都必须立即创建一次 Git 提交。
2. 不得把多个无关功能合并到同一个提交，也不得在功能完成后长期保留未提交改动。
3. 提交信息必须使用中文，并清楚说明改动目的；复杂改动应在提交正文写明实现方式和验证结果。
4. 提交前必须运行与改动范围相匹配的测试、构建或静态检查，并确认 `git diff --check` 通过。
5. 不得覆盖或回退用户已有的无关工作区改动。

## Agent 对话模块规则

1. `backend/app/modules/agent/`、`frontend/src/features/agent/`、用户端 Agent 页面、管理员端 Agent 监控、Agent 使用的模型配置及相关数据库迁移，都属于 Agent 对话模块。
2. 每次修改 Agent 对话模块时，必须同步更新 `docs/agent-conversation/` 中的教学文档。
3. 架构关系或数据流发生变化时，更新 `01-technical-panorama.md`。
4. 新增实现、修复故障或改变关键行为时，更新 `02-detailed-implementation.md` 中对应章节。
5. 每个 Agent 相关提交都必须在 `progress-log.md` 记录日期、目标、实现、验证和中文提交信息；日志与代码放在同一次提交中，提交哈希可在需要审计时通过该提交信息查询。
6. 文档面向项目维护者学习使用，不能只记录结果；必须解释为什么这样设计、请求如何流转、数据如何持久化，以及常见故障如何诊断。

## 数据库迁移规则

1. 数据库结构变化必须通过 Alembic 前向迁移完成，不能只修改 ORM 模型。
2. 不得使用 `alembic stamp head` 掩盖迁移失败或结构漂移。
3. 迁移应尽可能支持已有数据库安全升级，并补充迁移图或行为测试。
4. Agent 数据库故障的常用诊断和修复流程记录在 `docs/project-common-operations-guide.md`。

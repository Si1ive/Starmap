# 项目级开发规则

本文件作用于整个仓库。任何自动化开发代理或协作者在修改项目时都必须遵守。

## 提交规则

1. 每完成一个可独立描述、可独立验证的功能或修复，都必须立即创建一次 Git 提交。
2. 不得把多个无关功能合并到同一个提交，也不得在功能完成后长期保留未提交改动。
3. 提交信息必须使用中文，并清楚说明改动目的；复杂改动应在提交正文写明实现方式和验证结果。
4. 提交前必须运行与改动范围相匹配的测试、构建或静态检查，并确认 `git diff --check` 通过。
5. 不得覆盖或回退用户已有的无关工作区改动。

## 教学文档全局规范

1. 本节适用于仓库内所有面向维护者学习、排障和理解实现的教学文档，不限于 Agent 对话模块。
2. 教学文档只要涉及代码实现，就必须提供可快速定位的准确代码锚点，不得只写目录、模块名或模糊描述。
3. 每个代码锚点至少包含：仓库相对文件路径、类/函数/方法或常量的完整符号名、当前版本中的起止行范围；引用类方法时使用 `类名.方法名`，引用模块函数时使用函数名。
4. 推荐使用表格统一记录代码定位，格式为 `执行阶段 | 文件 | 符号 | 代码范围 | 职责`。例如：`创建一轮对话 | backend/app/modules/agent/router.py | create_turn | L170-L205 | 校验请求并调用时间线服务`。
5. 一个行为跨越多个函数、异步任务或前后端时，必须分别列出每一个关键代码锚点，并说明上一步传入什么、该函数执行什么、产生什么状态或返回值、下一步进入哪个函数。
6. 文档中的路径、符号名和代码范围必须以本次提交的最终代码为准。提交前应使用 `rg -n`、`nl -ba` 等方式重新核对；代码移动或函数重命名后，必须在同一次提交中更新失效的定位信息。
7. 代码范围应覆盖完成该职责所需的最小连续范围；不能用整个文件范围代替精确定位，也不能引用已经删除、仅计划新增或与描述不一致的代码。
8. 讲解代码时除说明“做了什么”外，还必须说明入口条件、关键参数、调用关系、数据库或外部系统副作用、错误如何传播以及最终结果在哪里被消费。

## 教学文档分卷与按需读取规则

1. 教学文档必须采用“路由索引 + 主题分卷”结构，禁止长期把架构、实现细节、故障记录和提交进展持续追加到一两个巨型 Markdown 文件中。
2. 每个教学文档目录必须维护 `README.md` 作为唯一阅读路由入口；README 只记录文档地图、适用场景、更新路由和迁移状态，不承载大段实现正文，建议控制在 200 行以内。
3. 稳定架构文档按执行主链或系统边界分卷；实现文档按领域职责分卷；一次性故障分析放入独立 `incidents/YYYY-MM-DD-主题.md`；进展日志按月写入 `progress/YYYY-MM.md`。不得把历史事故按时间顺序混入当前实现说明。
4. 单个主题文档软上限为 300 行或 30 KB，任一条件达到即应评估拆分；硬上限为 500 行或 50 KB，达到硬上限后必须先拆分，禁止继续追加正文。代码全景表因表格密度较高时仍受 50 KB 硬上限约束。
5. 自动化代理开始任务时，应先读取对应目录 README，再使用 `rg -n` 搜索主题、符号或故障关键词，只读取命中的相关分卷和必要行段；除非任务是全局架构审计或文档迁移，不得默认加载整个教学文档目录或所有历史进展。
6. 更新文档时只修改与本次行为变化直接相关的最小分卷；只有文档路由、职责边界或文件迁移发生变化时才更新 README。不得为了“同步文档”而重写无关分卷。
7. 同一事实只能有一个权威正文位置。README、全景和故障记录可链接该位置并写一至三句摘要，但禁止复制整段实现说明；迁移后旧文件应缩减为索引或兼容跳转，不得保留两份继续演进的正文。
8. 分卷不能削弱代码锚点要求。每个分卷仍必须遵守路径、完整符号、最终行范围、输入、处理、副作用、错误传播和最终消费位置等全局规范。
9. 进展日志只记录日期、目标、关键实现、验证和中文提交信息，不重复粘贴详细架构讲解；排查历史时先从月度日志定位提交，再进入对应领域分卷或故障单页。
10. 当现有文档已经超过硬上限时，下一次需要向该文件新增内容的任务必须先完成独立的文档拆分提交；拆分时应保留旧路径作为薄索引，修复相对链接，并使用 `rg -n`、`nl -ba` 重新校验迁移后的代码锚点。

## Agent 对话模块规则

1. `backend/app/modules/agent/`、`frontend/src/features/agent/`、用户端 Agent 页面、管理员端 Agent 监控、Agent 使用的模型配置及相关数据库迁移，都属于 Agent 对话模块。
2. 每次修改 Agent 对话模块时，必须先按 `docs/agent-conversation/README.md` 的更新路由定位最小相关分卷，并在同一提交中同步更新；不得默认读取或改写全部 Agent 文档。
3. 架构关系或数据流发生变化时，更新 `docs/agent-conversation/architecture/` 中对应执行主链分卷；新增实现、修复故障或改变关键行为时，更新 `docs/agent-conversation/implementation/` 中对应领域分卷；单次复杂故障另写入 `docs/agent-conversation/incidents/`。
4. 每个 Agent 相关提交都必须在当月 `docs/agent-conversation/progress/YYYY-MM.md` 记录日期、目标、实现、验证和中文提交信息；日志与代码放在同一次提交中，提交哈希可通过该提交信息查询。
5. 现有 `01-technical-panorama.md`、`02-detailed-implementation.md` 和 `progress-log.md` 已超过或接近分卷阈值，只作为迁移源；下一次需要新增正文前必须先完成独立拆分，并把旧路径改为薄索引。
6. 文档面向项目维护者学习使用，不能只记录结果；必须解释为什么这样设计、请求如何流转、数据如何持久化，以及常见故障如何诊断。
7. `architecture/` 中的代码执行全景必须从真实执行入口开始，按实际调用顺序列出完整主链：入口文件与入口函数、每一步调用的文件和函数、关键分支、事务边界、异步队列或 Worker 交接点、模型或外部服务调用、持久化位置、响应/SSE 返回位置以及前端最终消费函数。
8. 代码执行全景必须覆盖至少以下入口：用户端发起一轮对话、后端 HTTP 接收、Run/消息/Outbox 创建、Worker 执行、模型调用、结果持久化、事件投影与 SSE/前端归并；存在管理员配置、审批、用户补充输入等旁路时，也要分别给出入口到最终落点的调用链。
9. 全景中的每一步必须遵守“教学文档全局规范”的代码锚点格式，明确 `执行序号 | 文件 | 符号 | 代码范围 | 输入 | 处理 | 输出/副作用 | 下一步`，使维护者无需全文搜索即可从入口逐步定位到最终结果。
10. Agent 的入口函数、关键调用关系、异步边界或最终落点发生变化时，即使业务接口名称未变，也必须同步修订 `architecture/` 中对应全景分卷，禁止保留与当前代码不一致的历史调用链。

## 数据库迁移规则

1. 数据库结构变化必须通过 Alembic 前向迁移完成，不能只修改 ORM 模型。
2. 不得使用 `alembic stamp head` 掩盖迁移失败或结构漂移。
3. 迁移应尽可能支持已有数据库安全升级，并补充迁移图或行为测试。
4. Agent 数据库故障的常用诊断和修复流程记录在 `docs/guides/common-operations-guide.md`。

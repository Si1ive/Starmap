# 2026-07 文档治理与任务规划进展

## 2026-07-27：拆分接近硬上限的分层记忆任务单

- 目标：原任务单达到 330 行、49,084 字节后，先恢复“薄路由 + 主题分卷”，避免后续 MEM-007/008
  实现更新突破 50 KB 硬上限。
- 实现：保留原任务路径为状态总览，新增 `tasks/README.md`，并把已完成基础、记忆生命周期、管理端
  可观测性拆成三个权威主题分卷；总路由改为先进入任务目录 README。
- 验证：核对任务 ID、剩余项、相对链接、各文件尺寸与已迁移事实只保留一个权威位置；执行
  `git diff --check`。
- 提交信息：`拆分分层记忆整改任务单`

## 2026-07-26：细分记忆与 Validate 进展卷

- 目标：在 `01-memory-and-validation.md` 达到 300 行软上限后恢复按领域快速读取。
- 实现：保留旧路径为薄索引，把记忆基础/事实/Outbox 与 Validate 消费历史分别迁入 `01-memory-foundation-and-outbox.md`、`04-validate-memory-consumption.md`，并更新月份 README 路由。
- 验证：逐条核对 14 个历史标题只出现一次，检查相对链接、文件尺寸和 `git diff --check`。
- 提交信息：`细分记忆与 Validate 进展日志`

## 2026-07-26：拆分达到硬上限前的月度进展日志

### 目标

在 `progress/2026-07.md` 达到 496 行后，按教学文档硬上限规则先完成独立分卷，避免下一次 Agent 实现继续向接近 500 行的单文件追加正文。

### 实现

- 保留 `progress/2026-07.md` 为月度兼容薄索引，新增 `progress/README.md` 与 `progress/2026-07/README.md` 两级阅读路由。
- 按职责把历史正文机械迁移到记忆/Validate、RAG/Explain、文档治理/任务规划三个主题分卷，不改写历史实现、验证或提交信息。
- 更新 `docs/agent-conversation/README.md` 的目录地图和更新规则；已拆分月份只更新最小主题分卷，不再向兼容索引追加正文。
- 校验旧路径、总路由、月份路由和三个主题分卷之间的相对链接，并确认没有两份继续演进的权威正文。

### 验证

- 使用 `rg -n "^## "` 核对原 21 条历史进展全部迁移且各出现一次。
- 使用 `wc -l -c` 确认所有分卷低于 500 行和 50 KB 硬上限。
- `git diff --check` 通过。

### 提交信息

`拆分七月 Agent 进展日志主题分卷`

## 2026-07-25：完成 Agent 教学文档首轮分卷迁移

### 目标

按仓库级文档规则把 Agent 对话模块从旧的大型 Markdown 迁移为“README 路由 + architecture /
implementation / tasks / progress”结构，为后续 RAG、Explain 和记忆整改提供可持续的最小更新面。

### 实现

- 新建 `architecture/` 四个执行主链分卷，分别覆盖系统边界、用户对话主链、workflow 分支以及管理端/模型配置旁路。
- 新建 `implementation/` 七个实现分卷，拆开上下文、模型运行时、RAG、事件投影、管理端观测、前端体验和数据库迁移。
- 将 `01-technical-panorama.md`、`02-detailed-implementation.md` 和 `progress-log.md` 收敛为薄索引，并更新 README 当前状态与路由说明。
- 在任务单记录文档分卷迁移已完成，为后续 `FLOW-001` 等代码整改提供新的权威文档落点。

### 验证

- 使用 `rg --files docs/agent-conversation` 确认新目录与分卷全部落库。
- 逐个检查 README、旧路径索引和新增分卷之间的相对链接可达。
- `git diff --check` 通过。

### 提交信息

`拆分 Agent 对话教学文档分卷`

## 2026-07-25：校正分层记忆的稳定边界与外部基线

### 目标

回应对“分层记忆是否过度绑定当前 workflow”的审计质疑，补充主流 Agent 的公开记忆设计基线，并把
现有任务单改写成“稳定记忆内核 + 可变 workflow 适配层”的口径。

### 实现

- 更新 `tasks/2026-07-26-rag-explain-memory-remediation.md`，把 `MEM-004` 从“按工作流选择记忆”
  改为“按能力声明选择记忆”，明确 `MemoryNeed` 和 Bundle 的稳定边界。
- 把 `MEM-005` 改写为“Validate 是首个打通样例，不是记忆内核硬编码”，避免后续 workflow 重组时误读
  为必须连带重构记忆表结构。
- 把 `MEM-006` 改写为按事实事件回写，补充 Codex、Claude Code、Hermes 三类公开方案的对比和吸收点。

### 验证

- 重新使用 `rg -n`、`nl -ba` 核对任务单中的现有代码锚点未失效。
- 逐项检查外部基线部分是否都落在官方公开文档或官方仓库文档范围内。
- `git diff --check` 通过。

### 提交信息

`校正 Agent 分层记忆边界并补充外部设计基线`

## 2026-07-26：固化 RAG、Explain 与分层记忆待做计划

### 目标

把 `run_5c6c46d3` 的五项已定位问题和分层长期记忆实施方案写入可持续维护的任务文档，避免后续会话
压缩或切换导致任务、依赖和验收标准丢失。

### 实现

- 新建跨阶段任务单，逐条映射检索重试、Explain 无资料回答、Artifact 丢失、题目/知识点分类和二分查找检索问题。
- 记录已验证的 MySQL、稀疏召回、Qdrant 向量召回和失败装配层证据。
- 把分层记忆拆成事实边界、存储、输入增量、工作流选择、Validate 消费、输出回写、压缩治理和可观测任务。
- 在文档 README 增加 `tasks/` 路由和当前任务入口。

### 验证

- 逐条核对用户提出的五项问题均有任务 ID、状态、依赖和完成条件。
- 使用 `rg -n` 和 `nl -ba` 核对当前代码锚点。
- `git diff --check` 通过。

### 提交信息

`固化 Agent RAG 与分层记忆整改计划`

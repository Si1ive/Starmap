[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -m "not integration"
markers =
    integration: requires real external services and is excluded from the default suite
